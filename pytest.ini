[pytest]
# Only collect from tests/ so repo-root temp/runtime trees are never scanned.
testpaths = tests
norecursedirs = .git .hg .svn .tox .nox .venv venv env .eggs *.egg build dist .cache .pytest_cache __pycache__ .hypothesis .ai-loop .tmp tmpiemxgz0n node_modules
